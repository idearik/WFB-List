<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WFBali - Cafes</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <h1>Work-from-Cafe Friendly Places in Bali</h1>
  </header>
  <main>
    <% cafes.forEach(cafe => { %>
      <div class="cafe-card" data-cafe-name="<%= cafe.name %>">
        <h2><%= cafe.name %></h2>
        <p><%= cafe.area %></p>
        <p><%= cafe.hours %></p>
        <p><%= cafe.price %></p>
        <p><%= cafe.speed %></p>
        <p>Votes: <span class="vote-count"><%= cafe.votes %></span></p>
        <button class="vote-button">👍 Vote</button>
      </div>
    <% }); %>
  </main>
  <script src="/scripts.js"></script>
</body>
</html>
